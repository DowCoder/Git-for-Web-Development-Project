https://codepen.io/Dowcoder/pen/PoqRboL?editors=0110

1. What is Semantic HTML?
    HTML that gives meaning to our content such as nav, section, footer, h1-h6, etc.

    2. What is HTML used for? 
        HTML is used to create web pages that are displayed on the internet. Each page is connected to other pages called hyperlinks.
    3. What is an attribute and where do we put it? 
        They provide aditional information about the contents of an element. Atributes are used in the opening tag of an element.
    4. What is the h1 tag used for? How many times should I use it on a page?
        h1 is used for main headings. You can use it as many times but you don't want to over us the h1 tag. 
    5. Name two tags that have required attributes
    img: src, alt
    6. What do we put in the head of our HTML document? 
        Metadata (data about data)
    7. What is an id? 
        specifies a unique id for an HTML element.
    8. What elements can I add an id to? 
        
    9. How many times can I use the same id on a page?

        No because each id needs to be unique. 
    10. What is a class? 
        its used to define equal styles for elements 
    11. What elements can I add a class to? 
        you can add them to h1-h6, p, div, head, title, etc
    12. How many times can I use the same class on a page? 
        as many as you want, but when you go to style the class in css you will be designing the whole page with one class. 
    13. How do I get my link to open in a new tab?
        by using the anchor tag (a)
    14. What is the alt attribute used for? 
        its used for an alternate text for an image, if whoever is viewing your page and they cannot view your image the alt text will show up. 
    15. How do I reference an id?
        by using the "#" in front of the id in CSS.
    16. What is the difference between a section and a div
        the section tag defines sections in a document. div tag defines a division in an HTML document.
    17. What is CSS used for? 
        to deine styles for web pages, including design, layout and variations
    18. How to we select an element? Example - every h2 on the page
        h2 {}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
        id can be used to identify one element, where class can be used to identify more than one.
        <div id="nav"> 
        <header class="container">
        </header>
        </div>
    20. How do we select classes in CSS?
        .container
    21. How do we select a p element with a single class of “human””?
        p.human
    22. What is a parent child selector? When would this be useful? 
        selects all elements that are a direct child of the spefcified element. When you want to style all <p> inside the <div> parent.
    23. How do you select all links within a div with the class of sidebar?
        div .sidebar {}
    24. What is a pseudo selector?
        to define a special state of an element. ex: div .sidebar:hover{}
    25. What do we use the change the spacing between lines?
        line-height: 1.2;
    26. What do we use to change the spacing between letters?
        letter-spacing: 1px;
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
        text-transform: 
    28. How do I add a 1px border around my div that is dotted and black?
        div {
            border: 1px dotted black;
        }
    29. How do I select everything on the page? 
        * {}
    30. How do I write a comment in CSS?
        /* comment*/
    31. How do I find out what file I am in, when I am using the command line? 
    32. Using the command line - how do I see a list of files/folders in my current folder?
        ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
        rm -i filename,  because you can remove a file that can harm your computer.
    34. Why should I use version control?
        better collaboration 
    35. How often should I commit to github?
        when you complete part of the project.
    36. What is the command we would use to push our repo up to github? git push
    37. Walk me through Lambda's git flow. 
        1. Fork the project repo.
        2. add PM as collaborator
        3. clone forked repo
        4. go to terminal and type  git clone 'link to project'
        5. create a new branch with git checkout -b firstname-lastname
        6. open project and start coding
        7. when your done and ready to submit save your code and go back to terminal and type git add, git commit -m 'message', git push origin colby-dow
        8. go to your Github and click compare & pull request button
        9. choose forked repo as base repository
        10. add PM as reviewer and click pull request
        
Stretch Questions

    1. What is the difference between an inline element and a block element?
        block element- starts on a new line and take up the full width available
        inline element- does not start on a new line and only takes up as much width as necessary.
    2. What happens when an element is positioned absolutely? 
        its positioned to the nearest ancestor
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
        by using the box model
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
        <div>, <p>, <header>      <a>, <span>           <button>
    5. In your own words, explain the box model. What is the fix for the box model? 
        the box model wraps around every HTML element. the box model consist of margins, borders, padding and the HTML content.
        by using box-sizing:border-box